<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console Appender: Output logs to console -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger configuration: Define levels and appenders -->
    <logger name="com.sec.depchain.server" level="debug">
        <appender-ref ref="console" />
    </logger>

    <!-- Root logger: Set default logging level for the application -->
    <root level="info">
        <appender-ref ref="console" />
    </root>
</configuration>
